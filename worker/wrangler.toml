name = "runcoach-api"
main = "src/index.ts"
compatibility_date = "2024-03-01"

# Secrets (set via `wrangler secret put` or Cloudflare dashboard — never committed):
#   NOTION_API_KEY
#   NOTION_DB_ID
#   STRAVA_CLIENT_ID
#   STRAVA_CLIENT_SECRET
#   STRAVA_PLAN_START_DATE
#   ANTHROPIC_API_KEY
#   CF_ACCESS_CLIENT_ID      — service token client ID validated by the Worker
#   CF_ACCESS_CLIENT_SECRET  — service token client secret validated by the Worker

[[kv_namespaces]]
binding = "RUNNING_KV"
id = "f4df8303c25e4f1682c5c7d005a65ea1"
